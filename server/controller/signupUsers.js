export default class signupUsers {

    static createUser(req, res) {
        const { username,
                firstName,
                lastName,
                email,
                password,
                reEnterPassword } = req.body;
  
    }
}